<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <div class="body-wrapper">
      <header>
        <% include ../partials/header.ejs %>
      </header>
      <nav>
        <form action="/student">
          <input type="submit" class="underline" value="Main" />
        </form>
        <form action="/sessions?_method=DELETE" method="POST">
          <input type="submit" class="underline" value="Log Out" />
        </form>
      </nav>
      <main>
        <div class="show-box1">
            <h3><%=thisEntry.studentName; %></h3>
            <h3>ID: <%=thisEntry.studentID; %></h3>
        </div>
        <div class="header-box2">
          <ul class="entry-list">
            <li>Rotation: <%=thisEntry.rotation; %></li>
            <li>Skill: <%=thisEntry.skill; %></li>
            <li>Preceptor: <%=thisEntry.preceptor; %></li>
            <li>Proficiency: <%=thisEntry.levelOfControl; %></li>
          </ul>
        </div>
        <div class="edit-delete">
          <div class="button-edit">
            <form action="/student/<%=thisEntry.id; %>/edit" method="get">
              <input type="submit" value="Edit" />
            </form>
          </div>
          <div class="button-delete">
            <form action="/student/<%=thisEntry.id; %>?_method=DELETE" method="POST">
              <input type="submit" value="Delete" />
            </form>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
